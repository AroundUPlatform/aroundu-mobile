# AroundU — Runtime Environment Variables
# Add this file to .gitignore if it isn't already. Never commit API keys.

## Backend base URL for Android emulator (routes through 10.0.2.2 loopback)
#API_BASE_URL_ANDROID=http://10.0.2.2:20232
#
## Backend base URL for iOS simulator / physical device / web
#API_BASE_URL_OTHERS=http://localhost:20232

# Backend base URL for Android emulator (routes through 10.0.2.2 loopback)
API_BASE_URL_ANDROID=http://35.154.239.53

# Backend base URL for iOS simulator / physical device / web
API_BASE_URL_OTHERS=http://35.154.239.53

# Google Maps API Key
# Also paste this key into android/local.properties so native map tiles work in Android Studio.
GOOGLE_MAPS_API_KEY="AIzaSyAD578Rf3byRBfMURSimphge3sdzOTDdfE"

# Payload Encryption Key (Base64-encoded 32 bytes for ChaCha20-Poly1305)
# Generate with: dart -e "import 'dart:convert'; import 'dart:math'; final r = Random.secure(); print(base64Encode(List.generate(32, (_) => r.nextInt(256))));"
PAYLOAD_ENCRYPTION_KEY=dGhpcyBpcyBhIDMyIGJ5dGUga2V5ISEhMTIzNDU2Nzg=